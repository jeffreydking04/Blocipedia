<%= form_for([@wiki, @collaboration]) do |f| %>
  <div class="field">
    <%= f.select 'user_id', options_for_select(@users.collect{ |u| [u.email, u.id] }) %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>